---
type: Feature
---

It is now possible to customize the editable element tag name, classes, styles, and attributes via `config.root.element` or `config.roots.main.element`. The value can be a plain string (tag name) or an object:

```html
<template>
    <ckeditor :editor="Editor" :config="config" />
</template>

<script setup>
import { ref } from 'vue';
import { ClassicEditor } from 'ckeditor5';

const config = {
    root: {
        element: {
            name: 'article',
            classes: [ 'my-editor', 'custom-class' ],
            styles: { color: 'red' },
            attributes: { role: 'textbox' }
        }
    }
};
</script>
```

If no element definition is provided in config, the editable falls back to the `tagName` prop (`div` by default).
